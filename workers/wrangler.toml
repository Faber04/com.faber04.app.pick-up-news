name = "pickupnews-cors-proxy"
main = "cors-proxy.js"
compatibility_date = "2024-09-23"

# Deploy: npx wrangler deploy  (from the workers/ directory)
# After deploying, copy the worker URL and set it in .env.local:
#   VITE_CORS_PROXY_URL=https://pickupnews-cors-proxy.<your-subdomain>.workers.dev/?url=
